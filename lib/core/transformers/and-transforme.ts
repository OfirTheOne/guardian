import { LayerOperation } from "../../models/layer-operation";
import { SequentialLayer } from "../../models/sequential-layer";


// export const AndTransforme = (...operations : Array<SequentialLayer>): SequentialLayer => {

//     operations.reduce((transformed: SequentialLayer, op: SequentialLayer) => {
//         op.options
//         .


//     }, {} as SequentialLayer )
    
// }